import { FC } from "react";
import { BrowserRouter as Router, Switch, Route } from "react-router-dom";

const Routes: FC = () => {
  return (
    <Router>
      <Switch>
        {/* <Route path="/bridge" component={</>} /> */}
      </Switch>
    </Router>
  );
};

export default Routes;